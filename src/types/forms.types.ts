export type LoginFormData = {
    email: string;
    password: string;
}

export type RegisterFormData = {
    name: string;
    surname: string;
    email: string;
    dni: number;
    password: string;
    confirmPassword: string;
}